```bash
mkdir -p $HOME/tools
curl -L https://github.com/Ensono/stacks-cli/releases/download/v0.4.33/stacks-cli-$(uname \| tr 'A-Z' 'a-z')-amd64-0.4.33 -o $HOME/tools/stacks-cli
chmod +x $HOME/tools/stacks-cli
export PATH=$HOME/tools:$PATH
```